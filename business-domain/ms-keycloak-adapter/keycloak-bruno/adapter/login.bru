meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8084/login
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  username: diesgut
  password: diesgut
}

script:post-response {
  bru.setVar("TOKEN_BANK_API", res.body.access_token); //runtime variable
  bru.setGlobalEnvVar("TOKEN_BANK_API", res.body.access_token); //global variable
}
